#include <stdio.h>
#include <math.h>
#include <string.h>
#include <ctype.h>
typedef long long ll;
typedef double db;
int ktra(int n)
{
    int tmp = n % 10;
    n /= 10;
    while (n > 0)
    {
        if (tmp>=n)
    }
}
void solve()
{
}

int main()
{
    int t;
    scanf("%d", &t);
    getchar();
    while (t--)
    {
        solve();
        printf("\n");
    }
}